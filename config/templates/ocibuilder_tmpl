build:
  templates:
    {{ range .Templates }}{{ . }}
    {{ end }}
  steps:
    - metadata:
      name: {{ .ImageName }}
        labels:
          createdBy: spec-gen
      stages:
        {{ range .Stages }}{{ . }}
        {{ end }}
      tag: {{ .Tag }}
      daemon: true